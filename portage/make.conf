# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-O2 -pipe -march=native"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
RUSTFLAGS="${RUSTFLAGS} -C target-cpu=native"
# NOTE: This stage was built with the bindist USE flag enabled

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C.utf8
MAKEOPTS="-j8 -l11"
GENTOO_MIRRORS="https://mirrors.bfsu.edu.cn/gentoo https://mirrors.ustc.edu.cn/gentoo https://mirrors.nju.edu.cn/gentoo-zh"
L10N="zh zh-CN zh-TW zh-HK en en-US en-GB ja ja-JP"
I18N="${L10N}"
ACCEPT_KEYWORDS="amd64 ~amd64"
ACCEPT_LICENSE="*"
USE="wayland mimalloc alsa oss networkmanager zstd cups bluetooth \
     bluetooth-audio connection-sharing cjk opus webp nvenc cuda \
     bluetooth-sound zeroconf pulseaudio vaapi vdpau v4l bash-completion \
     zsh-completion fuse pipewire acpi screencast screen-sharing \
     mtp archive nfs samba google onedrive avif heif jpeg2k jpegxl \
     opencl scanner flatpak"
LUA_SINGLE_TARGET="lua5-4"
LUA_TARGETS="lua5-4"

# Use axel for downloading
FETCHCOMMAND="axel -n 16 --timeout=30 --alternate --no-clobber --output=\"\${DISTDIR}/\${FILE}\" \"\${URI}\""
RESUMECOMMAND="axel -n 16 --timeout=30 --alternate --no-clobber --output=\"\${DISTDIR}/\${FILE}\" \"\${URI}\""
